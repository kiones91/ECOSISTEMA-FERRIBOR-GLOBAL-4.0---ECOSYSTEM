FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM caddy:2-alpine
COPY --from=builder /app/../../dist/admin /srv
COPY <<EOF /etc/caddy/Caddyfile
:80 {
  root * /srv
  file_server
  try_files {path} /index.html
}
EOF
EXPOSE 80
