export default function ContactPage() {
	return (
		<section aria-labelledby="contact-title">
			<h1 id="contact-title">Contato</h1>
			<p>Área reservada para mapa, telefone, WhatsApp e chat.</p>
			<div aria-label="Blocos de contato" />
		</section>
	);
}